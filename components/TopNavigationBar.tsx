
import React from 'react';
// This component is no longer used as primary navigation is moved to the Header.
// Keeping the file for now to avoid breaking imports, but it renders nothing.

const TopNavigationBar: React.FC<any> = () => {
  return null; 
};

export default TopNavigationBar;
